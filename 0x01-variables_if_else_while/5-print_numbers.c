#include <stdio.h>
/**
 * main - print numbers 1 - 10
 * Return: (0)
 */
int main(void)
{
	int single_digit;

	for (single_digit = 0; single_digit <= 9; single_digit++)
	{
		printf("%d", single_digit);
	}

	printf("\n");


	return (0);
}
