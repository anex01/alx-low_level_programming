#include <stdio.h>

/**
 * main - lowercase alphabets in reverse
 * Return: (0)
 */
int main(void)
{
	int lower_case;

	for (lower_case = 122; lower_case >= 97; lower_case--)

	{
		putchar(lower_case);

	}
		putchar('\n');

	return (0);

}
